import fs from'node:fs/promises';import path from'node:path';import{createRequire}from'node:module';const r=createRequire(import.meta.url);let sharp;try{sharp=r('sharp')}catch{sharp=r(path.join(process.env.CODEX_PRIMARY_RUNTIME_NODE_MODULES,'sharp'))}await fs.mkdir('assets/thumbs',{recursive:true});for(let i=1;i<=108;i++){const b=String(i).padStart(3,'0');await sharp(`assets/scenes/${b}-a.webp`).resize(256,256).webp({quality:80}).toFile(`assets/thumbs/${b}.webp`)}console.log('108 lightweight previews generated');
