import http from 'node:http';import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(new URL('..',import.meta.url).pathname);const args=process.argv;const p=args.indexOf('--port');const port=Number(p>=0?args[p+1]:process.env.PORT||4173);
const mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.mjs':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json','.webmanifest':'application/manifest+json','.svg':'image/svg+xml','.webp':'image/webp','.png':'image/png'};
http.createServer((req,res)=>{let url;try{url=decodeURIComponent(new URL(req.url,'http://local').pathname)}catch{res.writeHead(400).end();return}const dest=path.resolve(root,'.'+url+(url.endsWith('/')?'index.html':''));if(!dest.startsWith(root+path.sep)){res.writeHead(403).end();return}fs.readFile(dest,(e,b)=>{if(e){res.writeHead(404).end('Not found');return}res.writeHead(200,{'Content-Type':mime[path.extname(dest)]||'application/octet-stream','Cache-Control':'no-cache'});res.end(b)})}).listen(port,'0.0.0.0',()=>console.log('Game server ready on '+port));
