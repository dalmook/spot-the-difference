# 검증 기록 · 2026-09-22

## 자동 검사

- core 단위 테스트7개 통과: 날짜별 도전 재현성,12개 테마 첫 장 해금, 다음 그림 해금, 재도전 보상/최고기록 유지, 저장 접근 오류와 손상된 JSON 복구, 판정/별 규칙, 일일 초기화, 가져오기 HTML 삽입 방지.
-108개 스테이지·12개 테마·216개 고유 이미지·540개 독립 정답 검사 통과.
- 모든 A/B 이미지의 픽셀을 대조: 바뀐 픽셀1,007,386개가 전부 지정된 정답 영역 안에 있음. 모든 정답은 RGB 최대 변화30이상인 픽셀250개 이상.
- JavaScript 구문 검사, 정적 배포 빌드 통과.

## 브라우저에서 확인

- 데스크톱 홈페이지·12개 테마·그림 목록 로딩.
- 첫 스테이지 실제5개 정답 클릭 →750점/5콤보/별3개/49코인/새 배지 표시.
- 같은 정답 연속 클릭시 점수·발견 수 중복 없음.
- 완료한 그림 별 표시와 다음 그림 해금, 나머지 잠김.
- 새 화면에서 저장된 별·코인·진행 상태 복원.
-390px 프레임 기반 모바일 반응형 검증: 가로 넘침 없음, 그림 두 장과 모바일 메뉴 표시.
- 모바일 힌트3→2 감소, 확대 상태 전환 및 동기화 위치 버튼 표시.

브라우저 실험 API인 WebMCP는 현재 검증 브라우저에 modelContext가 없어 실제 호출 검증이 불가했습니다. 기능 감지 후 등록하며 일반 게임 플레이에는 필요하지 않습니다.

실물 Android/iOS 기기와 모든108개 스테이지의 사람 눈에 의한 난이도 검수는 수행하지 않았습니다. 이미지 수·정답 수·정답 밖의 잘못된 차이는 전체 데이터 자동 검사로 검증했습니다.

추가 브라우저 확인: 모험 모드150초·하트5개로 시작, 일시 정지 중02:19에서 유지, 재개 후 카운트다운,12초 시간 정지 아이템 활성화 및 재사용 차단을 확인했습니다.
